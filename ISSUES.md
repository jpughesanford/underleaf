
## TODO 

- [x] the clone repo and + new project buttons are backwards. The former should be green and on the right and the latter should be white and one the left. We want the focus of the app to be on cloning existing repos
- [x] when a user clones a repo, there should be a field for naming the cloned repo something. That way it does not default to numbers. 
- [x] the repo link field should auto parse whatever is pasted in there for a valid repo link. It is likely ht user will paste in "git clone ..." Directly from GitHub. 
- [x] When error messages are given, make sure the user is able to select and copy the text to google the issue. 
- [x] why are there key emojis in the autofill window of the editor? Can the editor also autofill things like ref and autoref, \label, and so on?
- [x] can the pdf hyperlinks be made actively clickable?
- [ ] can we add the "Find in PDF" and "Find in Source" buttons that over leaf has that jump between the editor and pdf?
- [x] tab does not autofill from suggested autofill
- [x] find and replace tab looks awful, and doesn't work when replacing $\rho$ to something else 
- [x] move the save options menu to the settings menu and get rid of the button on the toolbar.
- [x] compile on cmd+s
- [ ] the git panel should copy VS code more closely, allowing users to check box the files they want to to commit
- [ ] clicking on a file in the fit panel should open the file with all git changes hi lighted. The PDF does not need to be here in the git panel. We can show the old file in a left editor and the new file in the right editor. 
- [ ] add spell check to editor. 
- [x] pdf viewer should not jump to top on compile, it should remember scroll location.
- [x] show dotted files button needs to show dotted folders too (not . And .. tho). All hidden things, basically. 
- [x] right clicking on a project in the homepage should allow you a rename option in the context menu. Moreover, clicking on the name of the project in the project view shouold let you edit it (thereby renamig the folder) to match overleaf
- [x] add a button that open the PDF in an external editor (preview of maybe the default internet explorer)
- [ ] Have the compiler error subwindow be a tab in the LEFTMOST panel (where file explorer and git is). If nothing has been compiled, no third tab should be shown. If something has been compiled, a tab is shown (but not necesaritly active). If errors occur, auto select the tab to be active. 
- [ ] the top bars of each subwindow need to be the same height. right now, the files toolbar is taller than the tabs in the editor window, and the toolbar in the PDF window. These subwindow should have standard height top bars, even tho each bar is different and has different functionality. 
- [ ] place a very feint image of the logo on the background of the Projects homepage behinds the cards. Make it look very aesthetic. Almost fully transparent but just a feint darker color. Use the svg logo so its crisp. 